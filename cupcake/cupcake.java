import java.util.Scanner;
public class SmallestNumber{
		
public static int getSmallest(int[] number){

	int smallest = [0]

	for(int count = 0; count < number.length; count++){
		if(number[count]) < smallest[count]){
			smallest = number[count];
		}
	return smallest;
	}
		
}


public static double getAverage(int[] number){

	int numberCount = 0;
	int sum = 0;
	int result = 0;

	for(int count = 0; count < number.length; count++){
		sum += number[count];
		numberCount++;
	}
	result = sum / numberCount;

	return result
}

public static int getOccurence(int[] number){

	int targetNumber = 4;
	int countTargetNumber = 0;
	

	for(int count = 0; count < number.length; count++){
		if(number[count] == targetNumber){
			countTargetNumber++;
		}
	}
	
		return countTargetNumber;

public static boolean getElement(int[] number){

	int targetNumber = 4;
	int countTargetNumber = 0;
	

	for(int count = 0; count < number.length; count++){
		if(number[count] == targetNumber){
			countTargetNumber++;
		}
	}
	
		return true;


public static int getFirstElement(int[] number){
	int firstElement = 0;
	for(int count = 0; count < number.length; count++){
		firstElement = number[0];
		 if(number[count] == []){
			return 0;
		 }	
	}

	}
	
	return firstElement;




public static int getLastElement(int[] number){

	int lastElement = 0;
	for(int count = 0; count < number.length; count++){
		lastElement = number[4];
		if(number[count] == []){
			return 0;
		}
	}
	
	return lastElement;


public static int getLength(int[] number){

	int targetNumber = 4;
	int countTargetNumber = 0;
	

	for(int count = 0; count < number.length; count++){
		if(number[count] == targetNumber){
			countTargetNumber++;
		}
	}
	
		return countTargetNumber;














}















	
	
}
